{% assign cardProduct = product %}

<div class="product-card--wrapper">
  <a href="{{cardProduct.url}}">
    <img
      src="{{cardProduct.featured_image | image_url }}"
      height="400"
      width="400" />
    <p>{{ cardProduct.title }}</p>
    
  </a>

</div>